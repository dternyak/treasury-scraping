# Test Environment Configuration
APP_NAME=FastAPI Render Boilerplate Test
ENVIRONMENT=test
DEBUG=true
LOG_LEVEL=DEBUG

# Test API Keys (use test/mock keys)
FIRECRAWL_API_KEY=test_firecrawl_key
GEMINI_API_KEY=test_gemini_key

# Test Database
POSTGRES_USER=test_user
POSTGRES_PASSWORD=test_password
POSTGRES_DB=test_db

# Test Redis
REDIS_HOST=redis
REDIS_PORT=6379
REDIS_DB=1

# Security (test keys)
SECRET_KEY=test-secret-key-not-for-production
ALGORITHM=HS256

# Reduced timeouts for testing
SCREENSHOT_TIMEOUT_SEC=30
SCRAPE_TIMEOUT_SEC=15
WORKER_DEFAULT_MAX_RETRIES=1

